package com.example.android_kotlin_lecture.Chapter_01

fun main() {
    // 3. 변수
    // val = value(값)
    // var = variable(변경 가능한)
    val a : Int = 3
    var b : Int = 10
    // a = 20 // val의 경우는 지정이 되고 나면 값을 재할당할 수 없음
    b = 20

    val name = "구다훈" // 코틀린에서는 변수의 타입을 명시적으로 지정하지 않아도 됨
}